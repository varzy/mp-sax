import { ComponentPlus } from '../../utils/component-plus';

ComponentPlus({
  properties: {
    tips: Array,
    withPrefix: { type: Boolean, value: true },
    useIndex: { type: Boolean, value: false }
  }
});
